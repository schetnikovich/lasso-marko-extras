/**
 * Empty file to allow requiring dependencies of this layout
 * in the following way:
 *
 * {
 *   "dependencies": [
 *     "require: src/layouts/base",
 *     ...
 *   ]
 * }
 *
 * When bundling a JavaScript module file, lasso will automatically
 * include any dependencies in a browser.json file in the same directory
 * (if it exists).
 */
